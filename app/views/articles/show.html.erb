<p>
  <strong>Title:</strong>
  <%= @article.title %>
</p>
 
<p>
  <strong>Text:</strong>
  <%= @article.text %>
</p>


    <p>Tags:
  <% @article.tags.each do |tag| %>
    <tr>
      <td><%= tag.name %></td>
          
<% end %>
</p>

<%= link_to 'Add Tags', new_tag_path %></br>
<%= link_to 'Delete Tags', tag_path %></br>

<%= link_to 'Edit Article', edit_article_path(@article) %></br>
<%= link_to 'Back', welcome_index_path %>
