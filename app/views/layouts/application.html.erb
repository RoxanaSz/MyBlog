<!DOCTYPE html>
<html>
<head>
  <title>MyBlog</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<p class="navbar-text pull-right">
<% if user_signed_in? %>
  Logged in as <%= current_user.role %> with <strong><%= current_user.email %></strong>.

  <%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link' %> |
  <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %>

 <li class="round-image-50"><%= image_tag(current_user.avatar.url(:medium)) %></li>

<!-- <%= image_tag current_user.avatar.url(:square)%> -->
</br>
<!--<%= image_tag current_user.avatar.url(:medium), :height => "50%" , :width => "50%"%>
 <tr>

	
<!--<%= image_tag("#{current_user.avatar_file_name}")  %>
 <%= image_tag "http://graph.facebook.com/#{current_user.id}/picture?type=small" %> -->
<% else %>
  <%= link_to "Sign up", new_user_registration_path, :class => 'navbar-link'  %> |
  <%= link_to "Login", new_user_session_path, :class => 'navbar-link'  %>
<% end %>


<%= yield %>

<% if notice %>
  <p class="alert alert-success"><%= notice %></p>
<% end %>
<% if alert %>
  <p class="alert alert-danger"><%= alert %></p>
<% end %>

</body>
</html>
